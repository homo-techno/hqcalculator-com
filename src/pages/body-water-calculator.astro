---
import CalculatorLayout from '../layouts/CalculatorLayout.astro';
import ShareButton from '../components/ShareButton.astro';
import FeedbackWidget from '../components/FeedbackWidget.astro';
import RelatedCalculators from '../components/RelatedCalculators.astro';
const title = "Body Water Calculator - Watson Formula (TBW)";
const description = "Calculate total body water using the Watson formula. Shows intracellular and extracellular water distribution.";
const relatedCalcs = [
  { name: "Blood Volume Calculator", url: "/blood-volume-calculator", description: "Estimated blood volume." },
  { name: "BMI Calculator", url: "/bmi-calculator", description: "Body mass index." },
  { name: "Hydration Calculator", url: "/hydration-calculator", description: "Daily water needs." },
  { name: "Body Surface Area Calculator", url: "/body-surface-area-calculator", description: "BSA calculation." },
  { name: "Sodium Intake Calculator", url: "/sodium-intake-calculator", description: "Daily sodium tracker." },
  { name: "Calorie Calculator", url: "/calorie-calculator", description: "Daily calories." },
];
---
<CalculatorLayout title={title} description={description} calculatorName="Body Water Calculator" keywords="body water, total body water, Watson formula, TBW, intracellular, extracellular" breadcrumbs={[{ name: "Body Water", url: "/body-water-calculator" }]}>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <h1 class="text-3xl sm:text-4xl font-extrabold text-text mb-2">Body Water Calculator</h1>
    <p class="text-text-secondary mb-8 max-w-2xl">Estimate total body water using the Watson formula.</p>
    <div class="bg-white border border-border rounded-2xl shadow-sm p-6 sm:p-8 max-w-2xl mx-auto">
      <div class="space-y-4">
        <div>
          <label class="block text-xs font-medium text-text mb-1">Sex</label>
          <select id="bw-sex" class="bw-in w-full px-4 py-3 border border-border rounded-xl text-lg">
            <option value="male" selected>Male</option>
            <option value="female">Female</option>
          </select>
        </div>
        <div class="grid grid-cols-3 gap-3">
          <div><label class="block text-xs font-medium text-text mb-1">Age (years)</label><input type="number" id="bw-age" class="bw-in w-full px-4 py-3 border border-border rounded-xl text-lg font-bold" value="35" min="1" max="120" step="1"></div>
          <div><label class="block text-xs font-medium text-text mb-1">Height (cm)</label><input type="number" id="bw-ht" class="bw-in w-full px-4 py-3 border border-border rounded-xl text-lg font-bold" value="175" min="50" max="250" step="1"></div>
          <div><label class="block text-xs font-medium text-text mb-1">Weight (kg)</label><input type="number" id="bw-wt" class="bw-in w-full px-4 py-3 border border-border rounded-xl text-lg font-bold" value="75" min="10" max="300" step="0.1"></div>
        </div>
        <div class="p-6 bg-primary rounded-2xl text-white text-center">
          <div class="text-sm text-blue-200 uppercase tracking-wide mb-1">Total Body Water</div>
          <div class="text-5xl font-extrabold font-mono" id="bw-total">0</div>
          <div class="text-sm text-blue-200 mt-1">liters</div>
        </div>
        <div class="grid grid-cols-3 gap-3">
          <div class="p-4 bg-surface-alt rounded-xl text-center"><div class="text-xs text-text-muted uppercase">% Body Weight</div><div class="text-xl font-bold" id="bw-pct">0%</div></div>
          <div class="p-4 bg-surface-alt rounded-xl text-center"><div class="text-xs text-text-muted uppercase">Intracellular</div><div class="text-xl font-bold" id="bw-icf">0 L</div></div>
          <div class="p-4 bg-surface-alt rounded-xl text-center"><div class="text-xs text-text-muted uppercase">Extracellular</div><div class="text-xl font-bold" id="bw-ecf">0 L</div></div>
        </div>
        <div class="p-4 bg-surface-alt rounded-xl">
          <h3 class="font-semibold text-sm text-text mb-3">Body Water Distribution</h3>
          <div class="grid grid-cols-2 gap-2 text-sm">
            <div class="flex justify-between"><span>Male (typical)</span><span class="font-bold">60% of body weight</span></div>
            <div class="flex justify-between"><span>Female (typical)</span><span class="font-bold">50% of body weight</span></div>
            <div class="flex justify-between"><span>Intracellular (ICF)</span><span class="font-bold">~2/3 of TBW</span></div>
            <div class="flex justify-between"><span>Extracellular (ECF)</span><span class="font-bold">~1/3 of TBW</span></div>
          </div>
        </div>
      </div>
      <ShareButton calculatorId="body-water" />
      <FeedbackWidget />
    </div>
    <article class="mt-12 max-w-4xl prose prose-slate mx-auto">
      <h2 class="text-2xl font-bold text-text">Watson Formula</h2>
      <p>The Watson formula estimates total body water (TBW). For males: TBW = 2.447 - 0.09156 x age + 0.1074 x height(cm) + 0.3362 x weight(kg). For females: TBW = -2.097 + 0.1069 x height(cm) + 0.2466 x weight(kg). Body water is typically divided into intracellular (2/3) and extracellular (1/3) compartments.</p>
    </article>
    <RelatedCalculators calculators={relatedCalcs} />
  </div>
  <script>
    function calcBW() {
      const sex = (document.getElementById('bw-sex') as HTMLSelectElement).value;
      const age = parseFloat((document.getElementById('bw-age') as HTMLInputElement).value) || 0;
      const ht = parseFloat((document.getElementById('bw-ht') as HTMLInputElement).value) || 0;
      const wt = parseFloat((document.getElementById('bw-wt') as HTMLInputElement).value) || 0;
      let tbw: number;
      if (sex === 'male') {
        tbw = 2.447 - 0.09156 * age + 0.1074 * ht + 0.3362 * wt;
      } else {
        tbw = -2.097 + 0.1069 * ht + 0.2466 * wt;
      }
      const pct = wt > 0 ? (tbw / wt) * 100 : 0;
      const icf = tbw * (2 / 3);
      const ecf = tbw * (1 / 3);
      document.getElementById('bw-total')!.textContent = tbw.toFixed(1);
      document.getElementById('bw-pct')!.textContent = pct.toFixed(0) + '%';
      document.getElementById('bw-icf')!.textContent = icf.toFixed(1) + ' L';
      document.getElementById('bw-ecf')!.textContent = ecf.toFixed(1) + ' L';
      document.dispatchEvent(new CustomEvent('calc-update'));
    }
    setTimeout(() => {
      document.querySelectorAll('.bw-in').forEach(el => { el.addEventListener('input', calcBW); el.addEventListener('change', calcBW); });
      calcBW();
    }, 50);
  </script>
</CalculatorLayout>
