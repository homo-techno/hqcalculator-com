---
import CalculatorLayout from '../layouts/CalculatorLayout.astro';
import ShareButton from '../components/ShareButton.astro';
import FeedbackWidget from '../components/FeedbackWidget.astro';
import RelatedCalculators from '../components/RelatedCalculators.astro';
const title = "Modulo Calculator - Remainder Division";
const description = "Calculate the modulo (remainder) of a division. Supports modular arithmetic operations.";
const relatedCalcs = [
  { name: "LCM & GCF", url: "/lcm-gcf-calculator", description: "LCM & GCF." },
  { name: "Prime Factorization", url: "/prime-factorization-calculator", description: "Primes." },
  { name: "Fraction Calculator", url: "/fraction-calculator", description: "Fractions." },
  { name: "Percentage Calculator", url: "/percentage-calculator", description: "Percentages." },
  { name: "Exponent Calculator", url: "/exponent-calculator", description: "Exponents." },
  { name: "Scientific Calculator", url: "/scientific-calculator", description: "Scientific." },
];
---
<CalculatorLayout title={title} description={description} calculatorName="Modulo Calculator" keywords="modulo calculator, remainder calculator, mod operation, modular arithmetic, division remainder" breadcrumbs={[{ name: "Modulo Calculator", url: "/modulo-calculator" }]}>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <h1 class="text-3xl sm:text-4xl font-extrabold text-text mb-2">Modulo Calculator</h1>
    <p class="text-text-secondary mb-8 max-w-2xl">Calculate a mod b — the remainder when a is divided by b.</p>
    <div class="bg-white border border-border rounded-2xl shadow-sm p-6 sm:p-8 max-w-2xl mx-auto">
      <div class="space-y-4">
        <div class="grid grid-cols-3 gap-3 items-end">
          <div><label class="block text-xs font-medium text-text mb-1">Dividend (a)</label><input type="number" id="mo-a" class="mo-in w-full px-4 py-3 border border-border rounded-xl text-2xl font-bold" value="17" step="1"></div>
          <div class="text-center text-2xl font-bold text-text pb-3">mod</div>
          <div><label class="block text-xs font-medium text-text mb-1">Divisor (b)</label><input type="number" id="mo-b" class="mo-in w-full px-4 py-3 border border-border rounded-xl text-2xl font-bold" value="5" step="1"></div>
        </div>
        <div class="p-6 bg-primary rounded-2xl text-white text-center">
          <div class="text-sm text-blue-200 uppercase tracking-wide mb-1">Result (a mod b)</div>
          <div class="text-5xl font-extrabold font-mono" id="mo-result">0</div>
        </div>
        <div class="grid grid-cols-2 gap-3">
          <div class="p-4 bg-surface-alt rounded-xl text-center"><div class="text-xs text-text-muted uppercase">Quotient</div><div class="text-xl font-bold font-mono" id="mo-quot">0</div></div>
          <div class="p-4 bg-surface-alt rounded-xl text-center"><div class="text-xs text-text-muted uppercase">Remainder</div><div class="text-xl font-bold font-mono" id="mo-rem">0</div></div>
        </div>
        <div class="p-4 bg-surface-alt rounded-xl">
          <h3 class="font-semibold text-sm text-text mb-2">Verification</h3>
          <div class="font-mono text-sm" id="mo-verify">a = b × q + r</div>
        </div>
        <div class="p-4 bg-surface-alt rounded-xl">
          <h3 class="font-semibold text-sm text-text mb-3">Divisibility Check</h3>
          <div class="text-sm" id="mo-div">-</div>
        </div>
      </div>
      <ShareButton calculatorId="modulo" />
      <FeedbackWidget />
    </div>
    <article class="mt-12 max-w-4xl prose prose-slate mx-auto">
      <h2 class="text-2xl font-bold text-text">Modulo Operation</h2>
      <p>The modulo operation finds the remainder after division: a mod b = r, where a = b × q + r and 0 ≤ r &lt; |b|.</p>
    </article>
    <RelatedCalculators calculators={relatedCalcs} />
  </div>
  <script>
    function calcMod() {
      const a = parseFloat((document.getElementById('mo-a') as HTMLInputElement).value) || 0;
      const b = parseFloat((document.getElementById('mo-b') as HTMLInputElement).value) || 1;
      if (b === 0) { document.getElementById('mo-result')!.textContent = 'Undefined'; return; }
      const remainder = ((a % b) + Math.abs(b)) % Math.abs(b);
      const quotient = Math.floor(a / b);
      document.getElementById('mo-result')!.textContent = String(remainder);
      document.getElementById('mo-quot')!.textContent = String(quotient);
      document.getElementById('mo-rem')!.textContent = String(remainder);
      document.getElementById('mo-verify')!.textContent = `${a} = ${b} × ${quotient} + ${remainder}`;
      document.getElementById('mo-div')!.textContent = remainder === 0 ? `${a} is divisible by ${b}` : `${a} is NOT divisible by ${b} (remainder: ${remainder})`;
      document.dispatchEvent(new CustomEvent('calc-update'));
    }
    setTimeout(() => { document.querySelectorAll('.mo-in').forEach(el => el.addEventListener('input', calcMod)); calcMod(); }, 50);
  </script>
</CalculatorLayout>
